# Core
streamlit==1.35.0
pandas
numpy
sympy

# Utilitaires
requests
python-dotenv
PyYAML

# Pour les rappels et mémorisation (si utilisés)
langchain==0.3.26
chromadb==1.0.15
tiktoken==0.9.0
openai

# PDF et parsing (si utiles dans ton projet)
PyMuPDF
pdfminer.six

# Pour gérer les erreurs de dépendances de Streamlit
tenacity>=8.1.0,<9
protobuf<5,>=3.20

# Optionnel : pour RAG, embeddings, etc.
faiss-cpu
